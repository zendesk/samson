<% help_text = 'Set the stages that must be deployed before this one for a particular reference.' %>
<%= check_box_section('Prerequisite Stages', help_text, :stage, :prerequisite_stage_ids, @project.stages - [@stage]) %>
