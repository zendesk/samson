<% if ENV['GCLOUD_IMAGE_SCANNER'] && @project.show_gcr_vulnerabilities %>
  <%= form.input :block_on_gcr_vulnerabilities, label: 'Block deploy when using vulnerable GCR images', as: :check_box %>
<% end %>
