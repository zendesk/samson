<%= form.input :build_with_gcb, label: 'build with GCB', as: :check_box, help: 'Build docker images using Google cloud builder, disables pushing to any other registry, disables pulling from other registries.' %>
