<%= form.input :kubernetes_rollback, as: :check_box, label: 'Rollback on failure' if @stage.kubernetes? %>
<%= form.input :kubernetes_reuse_build, as: :check_box, label: 'Reuse previous build', help: 'Speedup debugging when only changing kubernetes config' if @stage.kubernetes? && !@stage.production? %>
