---
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    project: some-project
    task: db-migrate
spec:
  template:
    spec:
      containers:
      - name: some-project
        image: docker-registry.zende.sk/truth_service:latest
        command: ["bin/rake", "db:migrate"]
      restartPolicy: Never
