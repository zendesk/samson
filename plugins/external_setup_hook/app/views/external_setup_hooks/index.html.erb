<% title = "Hooks" %>
<% page_title title %>

<h1><%= title %></h1>
<section class="clearfix">
  <div class="pull-right">
    <%= link_to "New", new_external_setup_hook_path, class: "btn btn-default" %>
  </div>
  <div class="clearfix"></div>
  <table class="table table-hover table-condensed">
    <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Endpoint</th>
      <th>Auth Type</th>
      <th>Verify SSL</th>
    </tr>
    </thead>
    <% @hooks.each do |hook| %>
      <tr>
        <td><%= link_to hook.name, hook %></td>
        <td><%= hook.description %></td>
        <td><%= hook.endpoint %></td>
        <td><%= hook.auth_type %></td>
        <td><%= hook.verify_ssl %></td>
        <td>
          <%= link_to_delete hook %>
        </td>
      </tr>
    <% end %>
  </table>
</section>
