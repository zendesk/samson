# frozen_string_literal: true
Gem::Specification.new "samson_aws_ecr", "0.0.0" do |s|
  s.summary = "Add Support for AWS ECR Docker registry"
  s.authors = [""]
  s.email = ""
  s.add_runtime_dependency "aws-sdk-ecr"
end
