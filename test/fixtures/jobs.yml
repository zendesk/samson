succeeded_test:
  command: cap staging deploy
  user: super_admin
  project: test
  status: succeeded
  output: This worked!
  commit: staging

succeeded_production_test:
  command: cap production deploy
  user: super_admin
  project: test
  status: succeeded
  output: This worked!
  commit: production

running_test:
  command: cap staging deploy
  user: super_admin
  project: test
  status: running
  output: This worked!
  commit: staging

failed_test:
  command: cap staging deploy
  user: super_admin
  project: test
  status: failed
  output: Error
  commit: staging
