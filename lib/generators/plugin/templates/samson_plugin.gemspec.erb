Gem::Specification.new 'samson_<%= file_name %>', '0.0.0' do |s|
  s.summary = 'Samson <%= name %> plugin'
  s.authors = ['<%= username %>']
  s.email = ['<%= email %>']
  s.files = Dir['{app,config,db,lib}/**/*']
end
