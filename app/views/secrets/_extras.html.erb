<% @secret.except(:key, :value, :visible, :comment, :deprecated_at).reject { |_, v| v.nil? }.each do |attribute, value| %>
  <%= form.input attribute do %>
    <%= render_secret_attribute(attribute, value) %>
  <% end %>
<% end %>
