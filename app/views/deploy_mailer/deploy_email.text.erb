<%= @deploy.summary %>

Deploy details: <%= project_deploy_url(@project, @deploy) %>

Risks:
<% if (risks = @changeset.pull_requests.select(&:risky?)) && risks.any? %>
  <% risks.each do |pr| %>
    #<%= pr.number %> <%= pr.title %> <%= pr.url %>
      <%= pr.risks %>

  <% end %>
<% else %>
  None
<% end %>

Commit Range: <%= @changeset.commit_range %>
<%= @changeset.github_url %>
