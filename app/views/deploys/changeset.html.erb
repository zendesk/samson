<%# no page_title since this is rendered via ajax %>

<% ChangesetFactory.attribute_tabs.each do |tab| %>
  <div class="tab-pane changeset-content" id="<%= tab %>">
    <%= render "changeset/#{tab}", changeset: @changeset, type: 'deploy' %>
  </div>
<% end %>
