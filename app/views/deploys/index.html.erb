<%= render partial: "deploy", collection: @deploys %>

<%= javascript_tag do %>
  $(document).ready(function() {
    $(".pre-scrollable").each(function(i, pre) {
      $(pre).scrollTop(pre.scrollHeight - 120);
    });
  });
<% end %>
