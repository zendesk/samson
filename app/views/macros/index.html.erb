<%= render 'projects/header', project: @project, tab: "macros" %>

<section class="tabs">
  <ul id="macros">
    <%= static_render @macros %>
  </ul>

  <% if admin_for_project? %>
    <%= link_to "Add Macro", new_project_macro_path(@project), class: "btn btn-default" %>
  <% end %>
</section>
