<tr>
  <td><%= user.id %></td>
  <td><%= user.name %></td>
  <td><%= user.email %></td>
  <td class="editable-role">
    <%= render partial: 'shared/project_role', locals: {user: user, project: @project} %>
  </td>
</tr>
